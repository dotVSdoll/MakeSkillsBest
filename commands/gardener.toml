description = "Run Context Gardener loop and open the Web Canvas garden"
prompt = """
Run the minimal Context Gardener loop for the current repository and map it to the local visual garden.

From the repository root, execute:

python -m src.main web . --open

Then report:
- the generated .gardener-state.json path
- the visualization URL
- the active layer and health score

If the Vite dependencies are missing, run `cd web && npm install` once, then retry the command.
"""
